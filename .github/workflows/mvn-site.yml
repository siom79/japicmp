name: mvn site

on:
  workflow_dispatch:

jobs:
  release:

    name: Site
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: Set up Maven Central
        uses: actions/setup-java@v2
        with:
          distribution: 'zulu'
          java-version: 17
          cache: maven
      - name: Generate website
        run: |
          mvn install site:site site:stage scm-publish:publish-scm
